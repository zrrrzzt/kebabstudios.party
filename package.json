{
  "name": "kebabstudios.party",
  "description": "Frontpage for Kebab Studios",
  "version": "1.0.6",
  "license": "MIT",
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "engines": {
    "node": ">=7.4.0"
  },
  "scripts": {
    "test": "standard && nsp check && tap --reporter=spec test/**/*.js",
    "test-offline": "standard && tap --reporter=spec test/**/*.js",
    "coverage": "tap test/**/*.js --coverage",
    "coveralls": "tap --cov --coverage-report=lcov test/**/*.js && cat coverage/lcov.info | coveralls",
    "setup": "npm install",
    "standard-fix": "standard --fix",
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "now-deploy": "npm test && now --npm && now alias"
  },
  "keywords": [
    "Kebab Studios",
    "Webpage",
    "Next.js"
  ],
  "repository": {
    "type": "git",
    "url": "http://github.com/zrrrzzt/kebabstudios.party.git"
  },
  "bugs": {
    "url": "http://github.com/zrrrzzt/kebabstudios.party/issues"
  },
  "greenkeeper": {
    "emails": false
  },
  "now": {
    "alias": "kebabstudios.party",
    "env": {
      "NODE_ENV": "production"
    }
  },
  "devDependencies": {
    "coveralls": "2.11.15",
    "nsp": "2.6.2",
    "standard": "8.6.0",
    "tap": "10.0.0"
  },
  "dependencies": {
    "next": "1.2.3"
  }
}
